import React from 'react';

const SelectCustom = ({ mutliple = false }) => {
  const [selected, setSelected] = React.useState(null);

  return <div className="select">test</div>;
};

export default SelectCustom;
